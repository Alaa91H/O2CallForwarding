package com.alaa.o2rufumleitung.ui.theme

import androidx.compose.ui.graphics.Color

/** Seed used for the static (non-dynamic) color scheme. */
val SeedBlue = Color(0xFF0D3B87)

// The two alternating card tones the option cards cycle through,
// each with a light and a dark-mode variant: gray, then blue-gray.
val CardGrayLight = Color(0xFFE9E9EC)
val CardBlueGrayLight = Color(0xFFE1E7F0)
val CardGrayDark = Color(0xFF2A2A2E)
val CardBlueGrayDark = Color(0xFF222A36)

val OnCardLight = Color(0xFF1B1B1F)
val OnCardDark = Color(0xFFE4E2E6)

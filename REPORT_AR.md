# تقرير تسليم تحديث O2 Call Forwarding

**تاريخ التسليم:** 25 أغسطس 2026
**نطاق التحديث:** اعتماد البريد الصوتي الرسمي لـ O2 كوجهة ثابتة وحيدة لجميع أنواع التحويل، مع حصر إدخال الرقم في التحويل المخصص وإكمال دعم اللغات المطلوبة.

## النتيجة التنفيذية

تم تصحيح منطق البريد الصوتي في التطبيق ليستخدم **333** كوجهة التحويل الجاهزة والافتراضية في كل بطاقات التحويل. يوضح دليل O2 الرسمي أن `333` هي الوجهة المختصرة لتحويل المكالمات إلى بريد O2 الصوتي؛ وتعرض O2 صراحةً الأكواد `**21*333#` للتحويل الدائم و`**004*333#` لتفعيل كل التحويلات الشرطية. [1]

> **تمييز مهم:** الرقم الطويل المبني من مقدمة الشبكة وـInfix يخدم الوصول المباشر إلى صندوق بريد رقم محدد، وخاصةً من خارج الهاتف أو أثناء التجوال. أمّا إعداد تحويل O2 القياسي إلى البريد الصوتي فيستخدم الوجهة المختصرة `333`. [1]

| المجال | النتيجة |
|---|---|
| وجهة بريد O2 الصوتي الافتراضية | `333` |
| البطاقات المشمولة | الدائم، عند الانشغال، عند عدم الرد، عند تعذر الوصول، وكل الحالات الشرطية |
| اللغات الجديدة المطلوبة | البولندية، الفرنسية، الإسبانية، الإيطالية، السويدية، البرتغالية البرازيلية |
| فحص الموارد | 12 ملف موارد Android، و35 مفتاح نصي متطابق في كل ملف |
| فحص جودة التغييرات | مرّ فحص XML، واتساق المفاتيح، وربط المنطق، وفحص مسافات Git بلا أخطاء |

## التغييرات المنفذة

أُضيفت قيمة مركزية باسم `O2_MAILBOX_SHORT_CODE = "333"` في `UssdManager`، ثم أُضيف مصدر الوجهة `O2_MAILBOX` في حالة الواجهة وجُعل القيمة الافتراضية لكل بطاقة. تعرض كل بطاقة اختياراً ثابتاً باسم **O2 Mailbox (333)** بلغـة الجهاز؛ وعند تفعيله تُمرر القيمة `333` إلى منشئ كود GSM/MMI، بلا إدخال رقم وبلا قراءة رقم البريد الصوتي من الشريحة.

تحتوي الواجهة الآن على خيارين فقط: **بريد O2 الصوتي (333)** و**رقم مخصص**. يظهر حقل إدخال الرقم حصراً عند اختيار التحويل المخصص، ولا يمكن أن يظهر ضمن مسار بريد O2 الصوتي.

| نوع التحويل | كود التفعيل الناتج مع زر O2 (333) |
|---|---|
| تحويل جميع المكالمات | `**21*333#` |
| عند الانشغال | `**67*333#` |
| عند عدم الرد | `**61*333#` |
| عند تعذر الوصول | `**62*333#` |
| كل الحالات الشرطية | `**004*333#` |

## الترجمة والتوطين

كانت البولندية موجودة مسبقاً، وتمت مراجعتها وإكمال مفتاحي زر بريد O2 الجديدين فيها. وأضيفت ملفات ترجمة كاملة للفرنسية (`fr`) والإسبانية (`es`) والإيطالية (`it`) والسويدية (`sv`) والبرتغالية البرازيلية (`pt-rBR`). كما أضيف مفتاحا الواجهة الجديدان إلى اللغات الموجودة سابقاً، لذلك لا تظهر عناصر الزر الجديد بلغة احتياطية داخل واجهة مترجمة.

| اللغة | محدد مورد Android | الحالة |
|---|---|---|
| البولندية | `values-pl` | محدثة ومكتملة |
| الفرنسية | `values-fr` | مضافة ومكتملة |
| الإسبانية | `values-es` | مضافة ومكتملة |
| الإيطالية | `values-it` | مضافة ومكتملة |
| السويدية | `values-sv` | مضافة ومكتملة |
| البرتغالية البرازيلية | `values-pt-rBR` | مضافة ومكتملة |

## استمرارية توقيع الإصدارات

أُضيف سر GitHub جديد باسم `KEYSTORE_CERT_SHA256` من دون تعديل الأسرار الحالية `KEYSTORE_BASE64` و`KEYSTORE_PASSWORD` و`KEY_ALIAS` و`KEY_PASSWORD`. يقرأ سير عمل الإصدار هذا السر بعد فك المفتاح، ثم يحسب بصمة شهادة المفتاح باستخدام `keytool` ويقارنها بالبصمة المحفوظة قبل بناء APK وAAB. يتوقف سير العمل فوراً إذا لم تتطابق الشهادتان، ما يمنع نشر تحديث موقّع خطأً.

تمت مقارنة شهادة APK المنشور في **v1.0.0** مع شهادة APK المنشور في **v1.1.0**؛ النتيجة متطابقة، لذلك يحافظ v1.1.0 على قابلية التثبيت كتحديث فوق v1.0.0. يتوفر أيضاً سكربت محلي `tools/verify_apk_signer.py` لاستخراج بصمة شهادة APK v2/v3 ومقارنتها في الإصدارات المستقبلية.

## التحقق والجودة

أضيف سكربت فحص محلي مستقل في `tools/verify_project.py`. لا يعتمد على مكتبات خارجية ولا ينفذ بناء Android؛ بل يتحقق من صحة XML، وغياب المفاتيح المكررة، وتطابق مجموعة المفاتيح بين كل ملفات الترجمة، ووجود كل ملفات اللغات، وربط زر O2 بالثابت `333` وبحالة الواجهة الافتراضية، وغياب مسار رقم البريد الصوتي المستخرج من الشريحة، وظهور الرقم اليدوي للتحويل المخصص فقط، واكتمال أكواد الأنواع الخمسة من التحويل.

> **نتيجة الفحص:** نجح تحليل 12 ملف موارد، وتحقق تطابق 35 مفتاحاً في كل لغة، وتحقق أن اختيار O2 الافتراضي يحلّ حصراً إلى `333`، وأن الحقل اليدوي يخص التحويل المخصص فقط، ونجح فحص `git diff --check` من أخطاء المسافات أو التصحيحات.

لا يمكن تأكيد استجابة شبكة O2 الفعلية أو عرض USSD داخل التطبيق إلا على هاتف Android حقيقي مزود بشريحة O2 وتغطية شبكة، لأن المحاكي لا يملك طبقة اتصال خلوي. ينصح بعد تثبيت APK على الهاتف بتجربة كل حالة واحدةً تلو الأخرى والتحقق من رد الشبكة قبل الاعتماد التشغيلي النهائي.

## محتويات التسليم

يتضمن أرشيف ZIP نسخة المشروع المعدلة بالكامل، بما فيها ملفات الموارد الجديدة، والتوثيق المحدث `README.md`، وهذا التقرير، وسكربت التحقق المحلي.

## المراجع

[1] [O2 Germany — Mailbox & Voicemail](https://www.o2online.de/service/mailbox-voicemail/)
